{
  "name": "closure-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright e2e tests for Closure redaction-review core flows",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
