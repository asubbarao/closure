FIRST read the shared build contract in full:
/private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md
Work in /Users/aloksubbarao/personal/closure.

YOU ARE THE BACKEND GROK. You own ALL of server/*.sql (ingest.sql, seed.sql,
routes.sql, load_templates.sql, app.sql). Do NOT edit anything under
server/templates/ or static/ except to confirm which tera context vars the
render macros must supply.

TASKS:
1. DELETE the shell/garbage: server/mutate.sh, server/export_case.sh,
   server/render_static.sql, and the stale static/*.html dumps
   (static/case_1.html, static/document_1_p1.html, static/index.html). Remove
   the shellfs LOAD and every read_text(... '| bash ...') / mutate.sh /
   export_case.sh reference from routes.sql and app.sql. shellfs must be gone.
2. Implement the full HTTP ROUTE CONTRACT from contract.md — the HTML page
   routes AND the JSON routes AND the three POST mutation routes AND the export
   route. Keep the existing HTML page rendering working (it already renders; do
   not regress it).
3. Runtime writes are DuckDB-native, no shell:
   - DECISIONS: each POST /api/suggestions/:id/decision and each fan-out from
     /api/entities/:id/decision appends ONE json file under exports/decisions/
     (write via DuckDB COPY (SELECT ... ) TO 'exports/decisions/<unique>.json'
     (FORMAT json, ARRAY false) — unique name from a sequence or uuid()). The
     status projection view reads them back with
     read_json('exports/decisions/*.json', ...) so the latest decision per
     suggestion wins. Rebuild v_suggestions to fold these in. Manual adds
     (POST /api/documents/:id/add) likewise persist as decision/added records
     and surface as suggestions with source='manual', born accepted.
   - EXPORT: POST /api/cases/:id/export runs pdf_redact per document over that
     doc's ACCEPTED suggestion boxes. read_pdf_words gives TOP-LEFT origin
     boxes; pdf_redact wants BOTTOM-LEFT — convert exactly once here:
     redact box = {page, x:x0, y:height_pt - y1, w:x1-x0, h:y1-y0}. Write to
     exports/<filename>_redacted.pdf. THEN prove it: re-run read_pdf/read_pdf_words
     over each output and assert ZERO occurrences of the redacted strings;
     return {exported:n, blocked:bool, flagged_remaining:n}. blocked=true when
     the case still has pending flagged-band suggestions (do not export those
     docs). Everything derived; NO hand-typed data.
4. quackapi route handlers are SELECT statements. If a POST needs to both write
   AND return JSON, and a plain "COPY ... TO" inside the handler works, use it;
   if the quackapi handler cannot execute a COPY/side-effecting statement,
   determine that empirically and REPORT it precisely (we will patch the
   quackapi C++ if required — do not fall back to a shell). Try:
   a table macro that performs the COPY and returns a status row.
5. Keep app.sql as the single entrypoint; it must still boot with the exact run
   command in the contract and end by serving on 8117 with static_dir := '.'.
   All derived tables via CREATE OR REPLACE CTAS. No SET VARIABLE. No VALUES.
   No INSERT INTO...SELECT for setup.

VERIFY (must actually run): boot with the contract's run command; curl each JSON
route; POST a decision and show the suggestion's status flips in
/api/documents/:id/suggestions; POST an export and show the returned counts +
prove a redacted SSN is unfindable in the output PDF via read_pdf. Report the
exact curls and their outputs, and list every file you changed/deleted. If any
template render macro needs a new context var, name it explicitly at the end.
