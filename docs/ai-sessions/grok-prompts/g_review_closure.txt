Deep review of the CURRENT closure app (/Users/aloksubbarao/personal/closure) —
the DuckDB+quackapi redaction-review tool. Boot it and actually exercise it (run:
/Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql"
serving :8117; fresh db; kill any prior listener). READ anything; WRITE only
/Users/aloksubbarao/personal/closure/docs/review-closure.md.

Produce four clearly-separated sections, concrete and ranked:
1. BUGS — real defects you can reproduce (broken routes, wrong coords, decisions
   that don't persist, render errors, race conditions, the 256MB serve memory cap
   in quackapi ApplyServeResourceGuards, etc.). Give repro + severity.
2. LIMITATIONS — architectural/honest constraints (OLTP write concurrency,
   unsigned quackapi, per-page render assumptions, no OCR for scanned PDFs yet,
   etc.). What breaks it and when.
3. TOP FEATURES — the highest-leverage features to ADD for the assignment's goals
   (fast triage of 800 suggestions, false-pos/neg handling, bulk, multi-doc,
   confidence, audit). Rank by impact vs effort.
4. ENHANCEMENTS — concrete UX/code improvements to what exists (specific files,
   specific changes).
Cite files/routes and what you observed running it. Be blunt.
