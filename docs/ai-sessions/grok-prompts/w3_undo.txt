FIRST read: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md . Work in /Users/aloksubbarao/personal/closure (server
restructured; decisions are append-only JSON files under exports/decisions/,
folded by views; mutations live in server/routes/decisions.sql). Run cmd: /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql"
YOU OWN: server/routes/decisions.sql (you are its owner now), NEW
server/routes/history.sql, NEW template partial history_*.html, NEW
static/history.js, minimal .read lines in app.sql.
BUILD VERSIONING + UNDO (Google-Docs-style history + Ctrl+Z):
1. BATCH IDS: every mutation POST (single decision, entity fan-out, band bulk,
   add-missed) stamps a batch_id (one per user action — a bulk accept of 119 is
   ONE batch with 119 decision records) + a human label ("Accepted 119 —
   Yasmine Nienow", "Rejected all 'Nienow Street' ×8"). Derive labels from the
   action, never hand-typed data.
2. UNDO (Ctrl+Z): POST /api/undo reverts the LATEST batch by APPENDING inverse
   decisions (restore each suggestion's prior status — computed from the log;
   never delete log records: the audit trail stays append-only and the undo
   itself is a recorded batch "Undid: <label>"). Repeated undo walks back batch
   by batch. Wire keyboard 'u' / Cmd+Z in the UI to it, with a toast.
3. VERSION TIMELINE: GET /api/cases/:id/history — the ordered batch list
   (label, actor, ts, counts, undone?). A history panel (Google-Docs-style
   right-rail timeline) in the UI; "restore to here" = append inverse decisions
   for every batch after that point (again: append-only, recorded).
VERIFY: boot; do single + bulk decisions; undo reverts the WHOLE bulk batch
(statuses re-checked via /api); history lists batches incl. the undo; restore-to-
point works; the decision-log file count only ever grows. Report evidence.
