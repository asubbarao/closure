FIRST read the shared build contract in full: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md
Work in /Users/aloksubbarao/personal/closure.

YOU ARE THE REVIEW-SURFACE GROK — the star screen (S2 in the design reference).
You own ONLY: server/templates/review.html and static/review.js. Do NOT edit any
.sql or other template.

Build the review workspace, matching the design reference's S2 quality:
- 3-column layout: left doclist (case documents, active highlighted, pending
  counts), center the page (real PNG /pages/<filename>/pN.png with absolutely-
  positioned overlay marks at the contract's pt->px transform, colored by
  status), right the suggestion queue grouped by entity with confidence values.
- The queue: entity group headers ("Yasmine Nienow · PERSON · SUBJECT · N in
  case" + "Accept all in case"), band filter chips (HIGH>=90 / REVIEW / FLAGGED
  with counts), each row shows matched text, context snippet, confidence, page.
- KEYBOARD-FIRST (the product's core): j/k move current suggestion (auto-scroll
  the page/canvas to it and across pages), a=accept, r=reject, u=undo, e=open
  entity bulk (link/redirect to /ui/bulk?entity=ID for now), n=enter add-missed
  (redirect to /ui/add-missed?doc=ID). Persistent keyboard legend footer.
- Each accept/reject/undo calls the contract POST routes via fetch(), updates the
  mark + queue row + progress meter in place (optimistic), and shows an <=8-word
  toast with an Undo affordance (strings from design/copy.md).
- A progress meter (reviewed / total) in the header.
Read the CURRENT server/templates/review.html first to see the tera context vars
the render macro already provides (doc, page, words, marks, suggestions, stats,
docs, case). Keep consuming those; if you need a new var, state it at the end.
Use fetch() to GET /api/documents/:id/suggestions for live status.

VERIFY: boot with the contract run command, open http://127.0.0.1:8117/documents/1,
confirm marks land on the page, press j/k/a/r/u and confirm the POST fires and
the UI updates. Report exactly what you observed. Do not touch design/ or samples/.


VERIFY-PORT NOTE: other agents may hold :8117. Boot your OWN instance on port 8121: run  sed "s/8117/8121/" server/app.sql > /tmp/boot_review.sql  then  /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned /tmp/review.db -c ".read /tmp/boot_review.sql" &  and curl/open http://127.0.0.1:8121 . Kill it when done.
