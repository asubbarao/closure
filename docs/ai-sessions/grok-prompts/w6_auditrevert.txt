SINGULAR REQUIREMENT: a human thoroughly clears 1000+ AI redaction suggestions across 1000+ pages, fast — via a funnel (auto-pass high-confidence + bulk groups, then hand-review the grouped residual), catching false negatives, with a full audit trail + revert.
Audit-trail + revertability review. READ server/routes/decisions.sql,
server/routes/history.sql, the decisions JSON-log pattern + folding views,
server/provenance.sql, and boot/undo/batch logic. WRITE ONLY
/Users/aloksubbarao/personal/closure/docs/audit-review.md. If the app boots
(/Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql", own port), exercise it; else review statically (boot may be under repair).
ASSESS the audit + revert story for LEGAL DEFENSIBILITY (this is law-enforcement
redaction — chain of custody matters):
- Append-only decision log: is every state change (accept/reject/add/undo/bulk/
  entity-fanout/export) captured with who/what/when/prior-state? Any path that
  mutates status WITHOUT an audit record? Any way to lose or rewrite history?
- Revert: batch undo (one bulk = one reversible batch), restore-to-point,
  Google-Docs-style history. Is undo itself audited (append inverse, never
  delete)? Can you always reconstruct the full timeline and the exact state at
  any past point? Edge cases: undo after export, concurrent decisions, undo of an
  undo.
- Provenance: source-PDF fingerprint at ingest + export lineage — does it prove
  the source wasn't altered mid-review and tie the redacted output to its source?
- Gaps + concrete fixes, ranked. And: what a court/compliance reviewer would want
  that's missing.
