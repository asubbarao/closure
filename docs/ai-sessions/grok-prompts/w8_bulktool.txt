FIRST read: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md , design/clozure-redaction-mocks.html (at /Users/aloksubbarao/Downloads/),
design/copy.md. Invoke your 'frontend-design' skill. Work in
/Users/aloksubbarao/personal/closure. App boots green: /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql" (kill :8117 first, rm
closure.db; verify on your OWN port by sed 8117->8141 into /tmp/boot_bulk.sql +
/tmp/bulk.db so you don't fight the running instance).

BUILD BULK AS ITS OWN FIRST-CLASS TOOL — a dedicated workspace, not an overlay.
This is THE throughput engine of the funnel: the human uses it to clear the ~2000
of ~3000 suggestions in a few actions, leaving only the residual for per-item
review. Route: GET /cases/:id/bulk (a full page). YOU OWN: NEW
server/routes/bulk.sql (page render macro + any bulk JSON endpoints not already
in routes/decisions.sql — REUSE the existing entity/band decision POSTs, do not
duplicate them), NEW server/templates/bulk_workspace.html, NEW static/bulk_tool.js,
minimal .read in app.sql. Do NOT touch review.html/review.js/triage.sql (another
agent owns those) or the existing bulk.html overlay (leave it).

THE TOOL:
- A single workspace listing EVERY bulk-actionable group across the case, in one
  scannable table: by ENTITY (name/SSN/phone/address, instance count, # docs), by
  CONFIDENCE BAND (high/review/flagged counts), and by REPEATED PATTERN. Each row:
  the value, how many instances, across how many documents, current
  accepted/pending/rejected split, and a confidence indicator.
- SCOPE selector per action: this document / all documents in case / a selected
  subset (multi-select docs — the "add 650-321-4121 to redactions on all files"
  case). Show the live count the action will affect.
- Batch ACCEPT / REJECT per group with a live count on the button ("Accept 128 —
  lay the ink"); FLAGGED items excluded from bulk by default with the banner;
  one audit batch per action (reuse the batch/undo machinery — every bulk action
  must be a single undoable batch, and an Undo affordance must be present).
- A FUNNEL summary at top: total → auto-passable (high-conf) → residual, with the
  one-button "Accept all high-confidence across case".
- Keyboard AND click parity; matches the design system (IBM Plex, tokens).
- Link to it prominently from the dashboard + review surface.

VERIFY (own port): render the page with real data; a group accept flips many
statuses across multiple docs (re-GET proves it); it creates ONE undoable batch
(history shows it, undo reverts it); flagged excluded. Report evidence + the route.
