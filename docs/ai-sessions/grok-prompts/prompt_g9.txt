Repo: /Users/aloksubbarao/personal/closure — a take-home submission (read "Alok_FDE_Fullstack Engineer Take-Home Assignment.txt" in the repo root first). It is an AI-powered PDF redaction review tool: one DuckDB process is the entire backend (database + HTTP API + PDF engine + HTML renderer) via the pdf, tera, and quackapi extensions; the UI is tera-rendered HTML matching the mockups in design/. The sample corpus in samples/ is fakeit-generated identities rendered to real PDFs via typst; suggestions are seeded by matching the case roster against real read_pdf_words coordinates; decisions are an append-only log (audit trail is structural, undo is an event); export uses pdf_redact with a proof pass that re-reads the output and finds zero hits.

YOUR TASK (G9, docs only — touch NOTHING except docs/rationale.md, README.md, .gitignore):

1. docs/rationale.md — the assignment's Part 3, ONE page. Sections per the assignment questions: core interaction model (keyboard-first queue triage + entity-level bulk decisions + why-cards on flagged items); alternatives considered and rejected (React+Node/FastAPI stack — rejected to keep one process and zero services; Figma/v0 for mocks — rejected for plain HTML/CSS); false positives/negatives handling and the reviewer's mental model (AI proposes, human disposes; flagged items are excluded from bulk-accept and require individual judgment; add-missed drag + find-all-matches covers false negatives); scaling story (100+ suggestions via bands+bulk; 50+ documents via entity fan-out; detection and geometry are set-based SQL); user assumptions (records clerk / detective, keyboard-comfortable, desktop, hours-long sessions); MVP vs ideal (what more time buys: OCR'd scans, multi-reviewer sign-off, version-pinned exports). Write tight, concrete, first person. NO marketing fluff.
2. README.md — rewrite: what it is (2 sentences), the run command (one line: the duckdb binary at /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql" — check server/app.sql exists first; if it doesn't exist yet, document `.read` of the server/*.sql files in order as the run path and note port 8117), repo layout table, how the sample corpus regenerates (samples/gen/), how tests run. Keep it under 80 lines.
3. .gitignore: ensure closure.db, exports/*.pdf, node_modules, .DS_Store, web/pages exclusion status stays consistent with what's committed (web/pages PNGs SHOULD be committed — do not ignore them).
Do not touch design/, samples/, server/, web/. Print a diff summary when done.
