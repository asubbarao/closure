FIRST read: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md , then docs/detection-design.md and server/remainder_scan.sql in
/Users/aloksubbarao/personal/closure. Server restructured: routes in
server/routes/*.sql, app.sql composes. Run cmd: /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql"
YOU OWN ONLY: server/remainder_scan.sql (finish/fix), NEW server/routes/remainder.sql,
NEW template partial(s) remainder_*.html, NEW static/remainder.js, plus the
minimal .read line(s) in app.sql.
BUILD: the automated false-negative catcher —
- remainder scan: words NOT covered by an accepted/pending suggestion, scanned for
  residual PII via regexp (SSN dashed/spaced/dotted, phones incl dotted, emails)
  AND fuzzy roster matching with the rapidfuzz community extension
  (INSTALL rapidfuzz FROM community — signed v1.5.4): rapidfuzz similarity between
  roster names (entities table) and remainder tokens/bigrams catches misspellings
  like "Robyn Prce". LIKE-family substring ops are banned; use regexp/position/rapidfuzz.
- Surface as a "Possible missed redactions" queue: route GET /api/documents/:id/missed
  (+ case-level), and a panel in the review UI with one-tap "add as redaction"
  (POSTs the existing add route with the found box).
VERIFY: boot; prove the scan catches the generated corpus's variant-form PII
(spaced/dotted SSN, misspelled witness) — show the rows; show one-tap add
creates an accepted manual suggestion. Report evidence.
