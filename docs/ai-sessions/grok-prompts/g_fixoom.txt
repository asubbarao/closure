Work in /Users/aloksubbarao/personal/closure. The DuckDB+tera+quackapi app boots
and serves (run: /Users/aloksubbarao/personal/quackapi/build/release/duckdb
-unsigned closure.db -c ".read server/app.sql" ; serves :8117), and /ui/bulk,
/ui/reject, /ui/add-missed return 200 — BUT the main pages "/", "/cases/1",
"/documents/1" and "/api/documents/1/suggestions" return HTTP 500 with DuckDB
"Out of Memory Error ... 244.1 MiB used". The render macros in server/routes.sql
build huge tera contexts (e.g. ALL words + ALL 951 suggestions for the 110-page
consolidated document) and blow a low memory ceiling.

FIX IT so every page returns 200 and renders real data:
1. Raise the memory ceiling: in server/app.sql set a sane DuckDB memory_limit
   (e.g. SET memory_limit='4GB'; SET max_memory='4GB';) BEFORE serving. Confirm
   what the current effective limit is and why it's ~244MiB (is quackapi/httplib
   capping it? a per-connection setting?). Report the root cause.
2. Make the review render LEAN regardless: the document review page must only
   build context for the CURRENT PAGE's words (not all pages), and the suggestion
   queue should be reasonably capped/paginated, so even the 110-page document
   renders fast without giant JSON contexts.
3. Verify with the app running: GET "/", "/cases/1", "/documents/1",
   "/documents/3" (the big one), and "/api/documents/1/suggestions" ALL return
   200 with real content; a POST to /api/suggestions/1/decision persists and the
   suggestion's status flips on re-GET. Use curl (fastest) to prove it; paste the
   status codes + a snippet of real rendered content.
Keep it pure DuckDB (no shell scripts). Touch only server/*.sql. Report root
cause, the exact changes, and the passing verification.
