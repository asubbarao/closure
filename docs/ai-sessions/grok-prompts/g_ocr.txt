You work in the user's own DuckDB 'pdf' extension repo:
/Users/aloksubbarao/duckdb-read_pdf  (functions: read_pdf, read_pdf_words,
pdf_info, pdf_to_png [poppler], write_pdf [libharu], COPY (FORMAT pdf),
pdf_redact, pdf_chunks). Read its README + src/ first.

GOAL: make the extension handle SCANNED / IMAGE-ONLY PDFs, because that is the
biggest real "this can't do PDFs" gap for a redaction tool — a scanned law-
enforcement PDF has NO text layer, so read_pdf_words returns nothing and NO PII
(false negatives) can be caught. Add OCR (tesseract) so image-only pages get a
text layer with word bounding boxes, using poppler to rasterize pages.

TASKS:
1. Assess current state: does read_pdf_words already fall back to OCR? Is
   tesseract linked/available? What does the ext do today on an image-only PDF
   (repro: rasterize a text PDF to images, rebuild as image-only, run
   read_pdf_words -> expect empty). Confirm poppler + libharu versions and
   whether tesseract is a dependency (vcpkg.json).
2. Build tesseract OCR support: a function (e.g. read_pdf_words_ocr or an ocr:=
   option on read_pdf_words) that rasterizes each page (poppler/pdf_to_png) and
   runs tesseract to get words + bounding boxes (tesseract gives word-level
   bboxes), normalized to the SAME coordinate convention read_pdf_words uses
   (top-left origin, PDF points). This is the unlock for redacting scanned docs.
3. Also confirm the AUDIT/redaction story: does pdf_redact truly remove content
   (verify a redacted string is unfindable after)? Is there any document-level
   provenance/audit in the ext, or must the app own the audit trail? Report.
4. Build with v1.5.x, standard signed community deps where possible. Provide a
   quick test proving OCR extracts text+boxes from an image-only PDF. Keep
   changes clean and minimal.
Report: what was missing, what you added, the new function signature, and OCR
proof on an image-only PDF. This is the stress-test-driven ext upgrade.
