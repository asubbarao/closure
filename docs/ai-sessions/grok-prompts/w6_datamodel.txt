SINGULAR REQUIREMENT: a human thoroughly clears 1000+ AI redaction suggestions across 1000+ pages, fast — via a funnel (auto-pass high-confidence + bulk groups, then hand-review the grouped residual), catching false negatives, with a full audit trail + revert.
Data-model review. READ server/schema.sql, server/ingest.sql (CTAS tables),
server/seed.sql, server/judge.sql, server/remainder_scan.sql, server/provenance.sql,
server/pdf_store.sql, and the decisions-log pattern (exports/decisions/*.json +
the folding views). WRITE ONLY
/Users/aloksubbarao/personal/closure/docs/data-model-review.md.
The schema was one-shot early and "seemed reasonable" — now assess it rigorously
against everything the app needs and might need:
- cases, documents, pages, words, entities, suggestions, the append-only decision
  log, judge votes, provenance/fingerprints, working-copy registry, batch/undo/
  history. Do the relationships, keys, and grain hold up? Any normalization
  issues, missing indexes/constraints, awkward joins, or places where the
  append-only-log projection is fragile?
- Future needs: multi-reviewer (actor/assignment), document batches/folders,
  OCR-sourced words, non-PDF documents (HTML/XML per webbed spike), redaction
  categories/policies, export versioning. Does the current schema absorb these
  cleanly or need changes?
- Propose CONCRETE schema changes (DDL sketches) ranked must/should/could, each
  with the reason. Keep it CTAS-friendly and honest about prototype vs prod.
Deliver a schema diagram (text), the review, and the ranked change list.
