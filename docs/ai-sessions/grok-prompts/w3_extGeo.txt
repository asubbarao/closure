Survey task. WRITE ONLY docs/ext-survey-geo.md (+ optional spike under
spikes/ext-geo/). Question: can the DuckDB geo/raster extensions help the
redaction app's RENDERING or geo-ish features, cheaply?
  raster (rasters in SQL — could page images/tiles be served or processed via
  raster instead of pre-rendered PNGs? tiling for huge pages?),
  spatial (core) + h3 + osmium + duckgl + cityjson (any real use: e.g. addresses
  in documents geocoded/visualized? redaction boxes as geometries — spatial ops
  like box-overlap/coverage via spatial predicates instead of hand-rolled
  arithmetic?).
Install what's available signed v1.5.4 osx_arm64, read functions, judge honestly.
The likeliest real win: representing suggestion/word boxes as spatial geometries
for overlap/containment queries (remainder scan coverage math) — spike that if it
holds up. Verdict list, mechanisms, one spike. If it's all a stretch, say so
plainly.
