THE SINGULAR REQUIREMENT of this product: a human must THOROUGHLY get through 800-3000 AI suggestions across 1000+ pages, fast. The funnel: ~3k suggestions -> high-confidence auto-pass + easy bulk groups clear ~2200 -> the human hand-reviews only the residual ~800, GROUPED (names, addresses, patterns) for batch judgment, while also catching false negatives. Everything must serve throughput + auditability.
Work in /Users/aloksubbarao/personal/closure. YOU OWN docs/rationale.md ONLY —
write the FINAL Part 3 design rationale, ONE PAGE (the assignment requires it;
read "Alok_FDE_Fullstack Engineer Take-Home Assignment.txt" Part 3 bullets and
answer each). Distill from: docs/architecture-thesis.md, punch-list.md,
scaling-and-limits.md, backend-oom-and-fastapi.md, pdf-stress.md,
detection-design.md, sanity-check.md, review-cleanroom.md, workflow docs.
STRUCTURE (tight, first person, no fluff):
1. Core interaction model — THE FUNNEL (3k suggestions -> auto-pass high-conf +
   bulk groups -> ~800 grouped residuals a human batch-judges; keyboard-first;
   false negatives caught by the automated remainder scan + add-missed).
2. Why this design / what I rejected — incl. the honest comparison: I also built
   three conventional Next.js/TS implementations clean-room as a control
   (review-cleanroom.md) and why the shipped design wins for throughput.
3. FP/FN handling + the reviewer's mental model (AI proposes, human disposes;
   flagged = judge-panel disagreement; nothing auto-redacts silently).
4. Architecture: one DuckDB process = DB + HTTP (quackapi CREATE ROUTE) + PDF
   engine + renderer — why, what it buys (no serialization boundaries, streaming
   /spill for 1GB PDFs), and its HONEST limits (single-writer OLTP, unsigned ext
   pending community submission, when I'd use Postgres+services in prod).
5. Scale: 100+ suggestions/50+ docs answer with the measured numbers (pdf-stress).
6. Assumptions about users; MVP vs ideal (what more time buys: real OCR flow,
   local-LLM judges, multi-reviewer sign-off).
ONE page. Every claim must trace to something built/measured in this repo.

VOICE / FRAMING (the author's real rationale — write it in his voice, different
words but this substance): "This is a prototype, and I was already building a
DuckDB PDF extension and quackapi (a FastAPI-class web framework inside DuckDB),
so collapsing the stack into one DuckDB process was the fastest path to a working
tool AND a real stress test of my own ecosystem. DuckDB: single reader/writer,
in-memory-fast with disk spill, near-zero dependencies, trivially easy deploy,
and I have extensive familiarity with the ecosystem." Prototype honesty: in prod
I'd reach for Postgres + a conventional app tier for multi-writer concurrency —
say so plainly.
NUMBERS: don't present the 3k->2200->800 funnel as measured — frame the scale
honestly: at 1000+ pages and ~1 redaction per page you're at 1000+ suggestions
minimum; use OUR corpus's real measured counts for concrete numbers and the
funnel as the design principle.
INPUT PARITY: keyboard-first but everything equally clickable; the revert/undo +
history (batch Ctrl+Z, restore-to-point, append-only audit) is a headline feature
— give it its own sentence.
