SINGULAR REQUIREMENT: a human thoroughly clears 1000+ AI redaction suggestions across 1000+ pages, fast — via a funnel (auto-pass high-confidence + bulk groups, then hand-review the grouped residual), catching false negatives, with a full audit trail + revert.
Deep tradeoff + gap analysis. READ + RUN anything; WRITE ONLY
/Users/aloksubbarao/personal/closure/docs/tradeoff-deep.md. Build on the existing
docs/tradeoff-analysis.md, docs/ux-compare.md, docs/review-cleanroom.md,
docs/scaling-and-limits.md — do not repeat them; go DEEPER on two questions the
author actually cares about:
1. WHERE DOES THE DUCKDB-MAXIMALIST APPROACH BREAK? The author's honest rationale
   is "I'm a DuckDB maximalist and already had a pdf ext + quackapi." Fine — but
   the RIGOROUS question is the failure/scaling boundary. Compare head-to-head vs
   the clean-room Next.js/TS apps (/Users/aloksubbarao/personal/closure-cleanroom/
   attempt-1,2,3) on: concurrent multi-reviewer writes (DuckDB single-writer),
   very large corpora (1GB+ / 10k pages — memory, spill, latency), many
   simultaneous users, long-running exports, deploy/ops. For each: at what point
   does the DuckDB app degrade or fail, what's the symptom, and what does the
   conventional stack do instead. Be quantitative where you can (run stress).
2. GAP IDENTIFIER: does any clean-room app DO something ours cannot or does not —
   a capability, an interaction, a robustness property? List concrete gaps with
   file pointers, ranked by whether they matter for the assignment.
End with: the honest 3-sentence "why DuckDB here" the author can defend in the
rationale (prototype speed + owned ecosystem + single-process simplicity) AND the
honest "in prod I'd change X" list.
