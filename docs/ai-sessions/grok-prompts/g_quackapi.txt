Capability / feasibility assessment. You may READ anything below; you may WRITE
only /Users/aloksubbarao/personal/closure/docs/quackapi-feasibility.md.

THE QUESTION: "Can we build this whole app purely on quackapi?" quackapi is the
user's DuckDB HTTP-server extension, and its repo is now COMPLETE/mature. Prove
whether it can serve the full redaction-review app end to end, and pinpoint any
gaps that would need an extension change.

STUDY, in this order:
1. quackapi (the server) — /Users/aloksubbarao/personal/quackapi : read README,
   src/ (routes/DDL, server, auth, state), and test/. Enumerate its FULL
   capability surface: route DDL (CREATE ROUTE, path/query/body params, typed
   validation), response types (JSON, HTML via column name, text, static files
   via static_dir), status codes, auth/API-keys/sessions, CORS, multipart/upload,
   streaming/SSE, redirects, openapi, concurrency model, and any limits (e.g. the
   memory cap, request size). Be exact — cite functions/DDL.
2. The current closure app — /Users/aloksubbarao/personal/closure/server/*.sql
   (routes, ingest, app) + server/templates/*.html + static/*.js : what HTTP
   surface + interactions it already relies on.
3. PEEK at the clean-room TS apps — /Users/aloksubbarao/personal/closure-cleanroom/attempt-1,2,3
   (Next/React/Tailwind). Read their route/handler list, data model, and the
   interactions they implement (bulk ops, add-missed, export, audit, confidence).
   This shows the FULL feature set a "proper" implementation includes. (Note: the
   clean-room apps must never see the closure repo — this peek is one-way, read
   only; do not modify them.)

DELIVER docs/quackapi-feasibility.md:
- A capability matrix: every feature the app needs (list them from #2 and #3) ->
  can quackapi do it today? (yes / yes-with-pattern / NO-needs-ext-change), with
  the specific quackapi mechanism or the specific missing piece.
- The honest gaps: anything quackapi canNOT do that a real app needs (e.g.
  streaming file downloads for a redacted PDF export, large-response memory,
  websockets, file upload for importing PDFs) — and what the ext change would be.
- Verdict: can the whole thing be built purely in quackapi? If yes, how; if not,
  exactly what to add to quackapi first (this is a punch-list for the ext).
Be concrete, cite paths/line numbers, run small experiments against a booted
quackapi if useful.
