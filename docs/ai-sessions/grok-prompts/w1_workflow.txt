Work in /Users/aloksubbarao/personal/closure. You OWN ONLY docs/workflow-improvements.md.
Boot the app fresh: kill any :8117 listener; rm -f closure.db; then
/Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql"
Wait for :8117. Now BE THE REVIEWER: you have 800 AI redaction suggestions across
a multi-document case and one hour. Walk the ACTUAL flows end to end via curl +
reading the served HTML/JS (dashboard -> library -> document review -> accept/
reject -> flagged why-card -> reject-all-matching -> add-missed -> bulk entity/
band -> export blocked -> resolve flagged -> export -> audit). At every step ask:
what slows me down, what's confusing, what's missing, what would make triage
FASTER? Write docs/workflow-improvements.md: a ranked list (impact-ordered) of
concrete workflow improvements, each with the exact interaction change and the
file(s) to touch. Focus on throughput ergonomics (keyboard, batching, defaults,
progressive disclosure), not visual polish. Be specific and honest about dead
ends you hit (they're bugs — flag them).
