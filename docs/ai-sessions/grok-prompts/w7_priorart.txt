Rigorous PRIOR-ART research for a design-rationale claim. Search the web
thoroughly. WRITE ONLY /Users/aloksubbarao/personal/closure/docs/prior-art.md.
THE CLAIM to verify or refute (do not assume it's true): "Collapsing the entire
web backend into a single EMBEDDED OLAP database process — where the HTTP server,
PDF engine, HTML template renderer, and data generation all run as in-process
loadable database extensions, and HTTP routes are declared as SQL DDL
(CREATE ROUTE) — is novel; there is no prior art for this exact instantiation."
Investigate the real landscape so the author neither overclaims nor underclaims:
- app-in-the-database precedents: Oracle APEX / PL-SQL Web Toolkit / mod_plsql,
  MS SQL Server SOAP/HTTP endpoints, PostgreSQL PL/pgSQL web attempts.
- DB-as-API-in-front-of: PostgREST, Supabase, Hasura, Datasette, pREST, Soul.
- DuckDB-specific: any existing DuckDB HTTP-server extensions (httpserver/httpfs
  serving, quack, evidence of anyone serving apps FROM DuckDB), DuckDB-Wasm apps.
- SQLite-as-server, "database as application server" writings, and the closest
  analogues.
For each: what it does, and precisely how it DIFFERS from this project's approach
(separate process vs in-process; OLTP heavyweight vs embedded OLAP; imperative
handlers vs SQL-DDL routes; whether PDF/render/gen are in the same engine).
CONCLUSION: state exactly what IS precedented (so the author cites it honestly)
and what — if anything — appears genuinely without prior art. Give the author 2-3
defensible, non-puncturable sentences for the rationale, with citations/links.
Be a skeptic: if it's less novel than claimed, say so plainly.

CRITICAL EXTRA DIMENSION (the author's key differentiator): the precedents
(PostgREST, Datasette, Hasura, Supabase, even Oracle APEX) are NARROW — they map
DB data to an API or serve pages. They do NOT give you a full APPLICATION RUNTIME
in the DB process. Assess whether ANY precedent lets the database process ALSO,
in-process:
  - make OUTBOUND HTTP calls to external APIs (this app: http_client / crawler),
  - RECEIVE arbitrary requests + run logic (quackapi CREATE ROUTE),
  - execute SHELL commands (shellfs),
  - CRAWL / parse the web (crawler, webbed html_to_json),
  - read/render/REDACT PDFs (the pdf ext),
  - GENERATE data (fakeit), fuzzy-match (rapidfuzz), hash (crypto), template
    (tera) — a whole toolbox, all as loadable extensions in ONE process.
The thesis to test: precedents turn a DB into an API endpoint; THIS turns an
embedded OLAP DB into a general-purpose application server + integration runtime
(inbound HTTP + outbound HTTP + shell + web + files + PDF + codegen), composable
in SQL. Is THAT precedented anywhere? That breadth is the real novelty claim —
verify or refute it specifically, and fold it into the defensible sentences.
