Work in /Users/aloksubbarao/personal/closure. You OWN ONLY spikes/marisa/** and
docs/marisa-verdict.md. Read anything; touch nothing else.
SPIKE: the 'marisa' DuckDB community extension (INSTALL marisa FROM community —
signed v1.5.4 osx_arm64 exists; study its functions first). Question: for
matching a LARGE PII dictionary (tens of thousands of names/streets/terms)
against document words at scale, does a marisa trie beat the current approach
(normalized equality join / n-gram join over a words table)?
Build a benchmark in spikes/marisa/: generate a big synthetic name list (fakeit)
+ use the app's real words table (boot or build from samples/*.pdf via read_pdf_words),
compare trie lookup vs hash join on time+memory at 10k/100k dictionary sizes.
Write docs/marisa-verdict.md: numbers, verdict (integrate / don't), and if
integrate — exactly where (the seed matcher? remainder scan?) and the SQL shape.
Honest: if the plain join wins (DuckDB hash joins are fast), say so.
