ADVERSARIAL failure-boundary testing. The author is a DuckDB maximalist and this
architecture (embedded DuckDB = DB + HTTP server via quackapi + PDF + renderer,
one process) is relatively NOVEL, so conventional wisdom doesn't tell us where it
breaks. YOUR JOB: empirically BREAK IT — push every axis to actual failure, record
the EXACT boundary, the symptom, and the root cause. Numbers, not theory. WRITE
ONLY /Users/aloksubbarao/personal/closure/docs/failure-boundaries.md.

App at /Users/aloksubbarao/personal/closure. Boot: kill :8117; rm -f closure.db;
/Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql" . quackapi source: /Users/aloksubbarao/personal/quackapi/src (read its
concurrency model, thread pool, memory guards). Use whatever load tooling works
(curl loops, a small load script, hey/ab if present, or DuckDB itself as a load
generator via http_client).

BREAK THESE AXES (for each: ramp until it fails, report the number where it
degrades/breaks, the symptom, and WHY):
1. CONCURRENT WRITES — the app's decisions are append-only JSON files, but if any
   path writes to closure.db under concurrent POSTs, DuckDB is single-writer. Fire
   N simultaneous accept/reject/bulk POSTs (N=2,10,50,200). Do writes serialize,
   block, error, corrupt, or lose data? Where's the wall?
2. HTTP CONCURRENCY — quackapi's httplib thread pool. Fire many simultaneous GETs
   (the heavy render routes + the 110-page doc). Concurrency at which latency
   explodes or requests drop. Does one slow SQL handler block the pool?
3. MEMORY / HUGE CORPUS — ingest a very large corpus (thousands of pages / 1GB+
   PDFs — generate via the pdf ext). Under the quackapi memory cap and with
   temp_directory spill: does it stream+spill or OOM? At what size? Peak spill.
4. LONG / HUNG QUERIES — the author noted hung queries are the main failure seen.
   Can one expensive route hang the whole server (no statement_timeout in 1.5.4)?
   Reproduce it; quantify blast radius (does it take down all routes?).
5. BULK FAN-OUT — an entity/band bulk decision over a huge suggestion set (the
   funnel accepting thousands at once). Time + memory; where it gets unusable.
6. STARTUP / STATE — routes live in in-memory quackapi state; what happens on
   crash/restart mid-review (decisions are files, but routes must re-register).
   Cold-start time as corpus grows.
For EACH axis: the concrete failure number, the symptom a user/operator sees, the
root cause (cite quackapi/DuckDB behavior), and the mitigation (config, code, or
"this is the ceiling — in prod use Postgres+services"). End with a one-screen
"FAILURE BOUNDARY TABLE": axis | breaks at | symptom | cause | mitigation. This is
THE honest scaling answer for the design rationale — be a pessimist, find the walls.
