Work in /Users/aloksubbarao/personal/closure. YOU OWN ONLY tests/e2e/**.
The server was restructured (server/routes/*.sql) and wave-2 agents are adding:
judge panel (GET /api/suggestions/:id/judges + UI on flagged items), a
"possible missed redactions" queue (GET /api/documents/:id/missed + one-tap add),
fuzzy add-missed search (exact vs similar counts), and a provenance/chain-of-
custody panel (GET /api/cases/:id/provenance). Those may still be landing —
write specs that tolerate absence gracefully (test.skip with a clear reason if a
route 404s) but FULLY exercise whatever is live.
1. First re-run the EXISTING suite against the current app (boot: rm closure.db;
   /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql" ; port 8117) — fix selector/route drift the restructure caused so the
   core-flow specs pass again (the corpus was regenerated: document filenames/
   counts changed — make specs data-driven from /api responses, not hardcoded
   names).
2. Add specs for the four new features above.
3. Run everything; report per-spec pass/fail/skip and the punch-list of real
   failures (app bugs, not test bugs).
