FIRST read the requirements doc:
/Users/aloksubbarao/personal/closure/"Alok_FDE_Fullstack Engineer Take-Home Assignment.txt"
and the shared contract:
/private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md
Work in /Users/aloksubbarao/personal/closure. Pure DuckDB.

You own NEW FILES ONLY: server/judge.sql, server/remainder_scan.sql, and
docs/detection-design.md. Do NOT edit server/routes.sql, server/ingest.sql,
server/app.sql, templates, or static (another agent owns those). Produce tables/
views other code will consume, and REPORT the exact ".read server/judge.sql" /
".read server/remainder_scan.sql" lines to add to app.sql (do not edit app.sql
yourself). All CTAS / views; the only writes are the app's append-only decisions
log (not yours).

PRODUCT FRAMING (build for THIS): overwhelming documents -> AI makes overwhelming
suggestions -> a human must triage 800 suggestions fast, catch misses, revert,
with an audit trail. Nobody reads verbose per-item rationale.

BUILD TWO THINGS:

1. CONFIDENCE AS A JUDGE ENSEMBLE (server/judge.sql):
   Replace single hardcoded confidences with a blended score from a simulated
   2-3 "judge" panel. Each suggestion gets N judge votes (deterministic, seeded —
   NO real LLM), each judge a (verdict: redact|keep|unsure, score, one-line
   reason keyed to a factor: pattern-match strength, surrounding-context,
   entity-type prior, cross-document corroboration). Aggregate to:
   - a final confidence % (blend of the judges),
   - a simple human-facing signal (agree / split / conflict),
   - the per-judge breakdown available on demand (for the UI to show 2-3 judges
     voting, not a wall of text).
   Split/conflict panels are exactly the flagged items needing human judgment.
   Expose as a view keyed by suggestion id. Keep it deterministic + pure SQL.

2. AUTOMATED FALSE-NEGATIVE CATCHER (server/remainder_scan.sql):
   Implement the "remainder scan": conceptually, apply the ACCEPTED redactions to
   a document, then scan what REMAINS for residual PII the AI missed. In SQL over
   the words table: take the words NOT covered by an accepted suggestion, and
   detect residual PII in that remainder using (a) the pdf ext / finetype type
   classification if available (INSTALL finetype FROM community — use it to
   classify tokens as SSN/PHONE/etc), and (b) a regex fallback for SSN
   (ddd-dd-dddd and spaced/dotted variants), phone, email. Emit a
   residual_pii_candidates view (document_id, page, box, text, kind, why) — these
   are surfaced to the human as "possible missed redactions". Prove it finds the
   corpus's planted false negatives (the spaced SSN / dotted forms) that the main
   detector misses.
   Substring ops (the LIKE family) are BANNED — use regexp_matches / finetype.

Write docs/detection-design.md explaining both, the human-facing model, and the
app.sql .read lines to wire it. Verify both against the real data (boot per the
contract, query the views) and report evidence: judge breakdown for a flagged
item, and residual candidates that include the planted misses.
