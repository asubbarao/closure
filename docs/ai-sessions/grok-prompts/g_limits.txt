Analysis for the design rationale of /Users/aloksubbarao/personal/closure (a
"DuckDB is the entire backend: DB + HTTP server via quackapi + PDF engine" app).
Read anything; only WRITE docs/scaling-and-limits.md. Run real experiments.

Answer, concretely and empirically (this is a prototype; the doc must be honest
about prod):
1. The "don't use DuckDB for OLTP" claim: WHY do people say it? Interrogate it.
   Test it here: concurrent writes (many simultaneous accept/reject POSTs),
   single-writer limits, write contention, MVCC/transaction behavior, latency
   under concurrent read+write. Where does it actually bite for THIS app
   (interactive decisions by a few reviewers) vs where it wouldn't?
2. Where do quackapi / quack (the HTTP layers) fail or limit us: the 256MB
   memory hard-cap (confirm in quackapi source, /Users/aloksubbarao/personal/quackapi/src),
   concurrency model / thread pool, no streaming responses, request size limits,
   auth/session gaps, the fact it's unsigned/unpublished.
3. Honest failure boundary: at what document scale, user concurrency, or data
   volume would this architecture break, and what's the symptom? Contrast with
   the conventional prod stack (Postgres + a real app server + object storage for
   PDFs) — what that buys and what it costs.
4. Write docs/scaling-and-limits.md: "prototype vs prod" framing, the tested
   OLTP verdict (with numbers), the concrete quackapi/quack limits, the failure
   boundary, and a crisp "if this were prod I would ..." recommendation. Be
   honest — if DuckDB genuinely scales fine for this workload, say so with
   evidence; if it breaks somewhere, pinpoint it.
Report the verdict + the numbers.
