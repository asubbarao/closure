THE SINGULAR REQUIREMENT of this product: a human must THOROUGHLY get through 800-3000 AI suggestions across 1000+ pages, fast. The funnel: ~3k suggestions -> high-confidence auto-pass + easy bulk groups clear ~2200 -> the human hand-reviews only the residual ~800, GROUPED (names, addresses, patterns) for batch judgment, while also catching false negatives. Everything must serve throughput + auditability.
FIRST read: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md . Work in /Users/aloksubbarao/personal/closure. Run cmd: /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql"
(own port: sed 8117->8134, /tmp/scans.db).
YOU OWN: server/pdf_io.sql, spikes/scans/**, docs/scanned-docs.md.
PROBLEM: not everything will be a born-digital PDF — many real court documents
are SCANS (image-only, no text layer). Today read_pdf_words returns nothing for
those -> no suggestions, no redaction, silent gap.
1. Assess the OCR path: the user's pdf extension repo (/Users/aloksubbarao/duckdb-read_pdf)
   recently gained tesseract OCR work (check its src/README for read_pdf_words
   ocr support or an ocr function; a local build may exist under build/release).
   The app currently uses the SIGNED community pdf build — determine whether OCR
   exists there; if only in the local build, loading it needs the same -unsigned
   path we already use for quackapi (acceptable).
2. Wire the capability: ingest detects image-only pages (0 words from a page with
   nonblank raster) and, when OCR is available, extracts OCR words+boxes into the
   same words table (flag source='ocr', maybe lower base confidence). When OCR is
   NOT available, the app must SAY so per document (a visible "scanned — no text
   layer" badge in the library/store), never silently show zero suggestions.
3. Test with samples/messy image-only PDF (generate one if missing).
VERIFY: boot; show an image-only doc either OCR-ingested (words>0, suggestions
appear) or clearly badged. Report which path landed and evidence.
