Work in /Users/aloksubbarao/personal/closure. Prove/expose the limits of the
PDF pipeline (the user's community 'pdf' extension: read_pdf_words, pdf_info,
pdf_to_png, write_pdf, COPY ... (FORMAT pdf), pdf_redact). This feeds the design
rationale: exactly WHERE and WHY pdf handling fails.

You OWN: a new tests/stress/ harness (DuckDB .sql only, no python/shell hacks),
generated corpora under samples/stress/, and the report docs/pdf-stress.md. Do
NOT edit server/*.sql, templates, or static (another agent owns those).

GENERATE (pure DuckDB + pdf ext) and TEST:
1. A ~1GB / many-thousand-page PDF (write_pdf / COPY FORMAT pdf on huge text).
   Confirm pdf_info; then read_pdf_words + pdf_to_png a page under a TIGHT budget
   (SET memory_limit='512MB'; SET temp_directory='/tmp/closure_spill'). Does it
   stream+spill or OOM? Numbers: pages, bytes, peak spill, wall time.
2. A 100+ page doc and a FOLDER of many PDFs (glob ingest samples/stress/*.pdf).
   Measure ingest time + memory as file count scales.
3. FAILURE MODES — find what the pdf ext canNOT handle and document each with a
   concrete repro: encrypted/password PDFs, image-only/scanned PDFs (no text
   layer — read_pdf_words returns nothing → needs OCR), malformed/truncated PDFs,
   non-latin/CJK text, rotated pages, forms/annotations. For each: what happens
   (error? empty? wrong coords?) and the implication for the app.
4. Write docs/pdf-stress.md: a table of scenario -> result -> verdict, the
   headline scale numbers, the honest list of "this can't do PDFs if X" cases,
   and recommendations (OCR fallback, streaming rules, size guards).
Report the headline numbers + the failure list.
