THE SINGULAR REQUIREMENT of this product: a human must THOROUGHLY get through 800-3000 AI suggestions across 1000+ pages, fast. The funnel: ~3k suggestions -> high-confidence auto-pass + easy bulk groups clear ~2200 -> the human hand-reviews only the residual ~800, GROUPED (names, addresses, patterns) for batch judgment, while also catching false negatives. Everything must serve throughput + auditability.
FIRST read: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md . Work in /Users/aloksubbarao/personal/closure.
YOU OWN: samples/gen/**, scripts/** (setup.sh / generate-samples.sh), server/ingest.sql,
and the samples outputs. PRINCIPLE: the REPO IS SMALL — committed PDFs minimal or
none; the person clones and runs scripts/setup.sh which GENERATES everything via
a fakeit SQL query (pure DuckDB: fakeit + pdf ext write_pdf/COPY FORMAT pdf; NO
python/typst):
1. setup.sh -> duckdb runs samples/gen/generate.sql: parameterized (N cases, docs
   per case, pages), producing the 110-page consolidated doc, the multi-document
   batches per case, variant-form FN PII + surname FP bait emergent in the data
   (no answer-key reads at runtime), and the messy set (image-only, encrypted,
   truncated, rotated, CJK).
2. FILE MANAGER GLOB ASSOCIATION: ingest should discover documents via glob
   patterns (pdf ext glob-reads) and associate them to cases by filename pattern
   from the manifest — make server/ingest.sql robust to whatever the generator
   produces, failing loudly on desync (keep the boot asserts).
3. Also render page PNGs into pages/ as part of setup.sh (pdftoppm loop or
   pdf_to_png) so clone->setup->run gives a full working app.
VERIFY from a simulated fresh clone (rm samples/*.pdf pages/ etc.; run setup.sh;
boot: /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql" sweep routes 200; counts sane; 110-pager present). Report the exact
clone->setup->run sequence and proof.
