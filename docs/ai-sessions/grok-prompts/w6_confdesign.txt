SINGULAR REQUIREMENT: a human thoroughly clears 1000+ AI redaction suggestions across 1000+ pages, fast — via a funnel (auto-pass high-confidence + bulk groups, then hand-review the grouped residual), catching false negatives, with a full audit trail + revert.
DESIGN DOC (may not be implemented — ML is explicitly NOT the assignment's goal,
but a thoughtful design shows judgment). READ docs/detection-design.md,
docs/llm-judge.md, server/judge.sql. WRITE ONLY
/Users/aloksubbarao/personal/closure/docs/confidence-design.md.
GET THE CONCEPTUAL MODEL EXACTLY RIGHT (the author stated it):
- Confidence attaches to AI-GENERATED SUGGESTIONS (things the AI proposed to
  redact). 
- A FALSE POSITIVE is a suggestion that should NOT be redacted — it EXISTS and
  has a confidence, so low confidence / judge disagreement is precisely how we
  SURFACE likely false positives for human review. (You "find" FPs via low
  confidence + split judge votes.)
- A FALSE NEGATIVE is missed PII — NO suggestion exists, so it has no confidence
  and judges can't score it; FNs are caught by the remainder-scan (residual PII
  after accepted redactions), NOT by the confidence system. Say this clearly.
DESIGN: a 2- or 3-judge ensemble that produces a per-suggestion confidence, fast
enough to matter, that (a) blends signals (pattern strength, surrounding context,
entity-type prior, cross-document corroboration) into a 0-100 score + agree/split/
conflict, and (b) optionally escalates only SPLIT/low items to a local-LLM judge
(ollama, per docs/llm-judge.md — async, off the hot path). Show how this powers
the FUNNEL: high-confidence + agree → auto-passable; split/low → the human's
grouped residual queue. Cover: what each judge sees, how votes combine, latency
budget, when the LLM tier fires, and how it surfaces in the UI (2-3 chips + a
gradient — humans don't read essays). Note what's already built (deterministic
ensemble in judge.sql) vs proposed. Honest about it being a heuristic, not ML.
