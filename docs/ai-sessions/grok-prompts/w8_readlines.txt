Spike + integrate read_lines for line-level addressing in the redaction app at
/Users/aloksubbarao/personal/closure. YOU OWN: spikes/readlines/** and a NEW
server/lines.sql + docs/line-addressing.md (do NOT edit routes/templates/other
server modules — propose the .read line for app.sql at the end).
The 'read_lines' community ext (INSTALL read_lines FROM community) + read_lines_lateral
gives a line_number on every line; the pdf ext also has pdf_grep (page+line+text).
GOAL: line-level addressing so the app can (a) cite a finding as "p.3, line 14"
in the audit log + missed-redactions queue (far more human than raw coords), and
(b) support "snap-to-line" / highlight-whole-line in the UI (select a line ->
redact the whole line's span).
1. Spike: over samples/*.pdf, derive per (document,page,line_number) the text +
   the merged bounding box of that line's words (min x0,y0 / max x1,y1 from
   read_pdf_words grouped into lines by y0). This gives every line a box for
   snap-to-line redaction and a line_number for citation.
2. Build server/lines.sql: a lines view (document_id, page_no, line_no, text,
   x0,y0,x1,y1) usable by the missed-scan (cite line), the audit log (p.N line M),
   and a future snap-to-line redact (redact the line's box).
3. docs/line-addressing.md: what read_lines/read_lines_lateral buys, the line
   view shape, the two UI wins (citation + snap-to-line), and how to wire it
   (routes/UI hooks for another agent). Prove the line view on real data (show
   'p.3 line 14 = <text>' for a known PII line).
Report the spike output + the proposed app.sql .read line.
