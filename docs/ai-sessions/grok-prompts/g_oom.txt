Investigation + honest architecture assessment. Write findings to
/Users/aloksubbarao/personal/closure/docs/backend-oom-and-fastapi.md. You may
read anything; only WRITE that one doc (plus you may run the app / experiments).

CONTEXT: the app at /Users/aloksubbarao/personal/closure is "DuckDB is the whole
backend" — DuckDB + tera + the local 'quackapi' HTTP-server extension
(source: /Users/aloksubbarao/personal/quackapi/src). It just threw
"Out of Memory Error ... 244.1 MiB used" on the main render routes. A prior fix
set memory_limit='4GB' in server/app.sql and pages now return 200, but a note
says "quackapi_serve() forcibly re-SETs memory_limit TO '256MB'". The owner is
(rightly) spooked: OOM is scary in the very thing meant to SCALE to 1GB+/
thousands-of-pages PDFs.

PART 1 — ROOT-CAUSE THE OOM (be precise, cite code):
- Find where/why quackapi caps memory_limit to ~256MB (grep the quackapi C++
  source). Is it per-connection? does each HTTP worker get its own limited
  context? Does the app.sql SET actually stick, or does serve() override it per
  request? Confirm empirically.
- Determine the REAL memory behavior: with a page-scoped render, what's peak
  memory? Does DuckDB stream read_pdf_words and spill to temp_directory, or does
  something force full materialization? Is the 4GB set a genuine fix or a band-aid
  that will still blow up on a 1GB / 10k-page PDF?
- Give the correct fix (e.g. patch quackapi to not hard-cap memory / make it
  configurable + set temp_directory for spill + keep renders page-scoped).

PART 2 — FASTAPI EQUIVALENCE (honest, no bias toward DuckDB):
- Sketch the equivalent backend as FastAPI (Python) serving the SAME JSON/HTML
  contract the frontend needs, with DuckDB or SQLite as the query engine behind
  it (NOT as the web server). What does it look like, how many moving parts?
- Compare on the axes that matter here: memory safety under huge PDFs, streaming,
  concurrency, operational simplicity, deployability, and how much of the current
  work (schema, detection SQL, pdf extension usage) carries over unchanged.
- For the 1GB+/thousands-of-pages stress case specifically: which architecture is
  more likely to stay memory-safe and why? (Note: FastAPI+DuckDB-as-library still
  uses DuckDB's engine and its spill-to-disk — the question is whether putting the
  HTTP server INSIDE DuckDB via quackapi is the liability.)
- VERDICT: keep quackapi (with the fix), or move the HTTP layer to FastAPI/Fastify
  and keep DuckDB as the embedded engine? Give a clear recommendation with the
  reasoning, and the smallest change that de-risks the OOM either way.

Be concrete, cite file paths/line numbers, run experiments to back claims.
