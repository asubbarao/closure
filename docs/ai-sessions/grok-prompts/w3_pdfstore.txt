FIRST read: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md and /Users/aloksubbarao/personal/closure/docs/code-quality.md
(PDF-handling section) and spikes/provenance/README.md if present.
Work in /Users/aloksubbarao/personal/closure (server restructured: modules in
server/, routes in server/routes/, app.sql composes). Run cmd: /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned closure.db -c ".read server/app.sql"
YOU OWN ONLY: NEW server/pdf_store.sql, NEW server/routes/store.sql, minimal
.read lines in app.sql, and a NEW docs/pdf-lifecycle.md. READ but do not write
server/pdf_io.sql and server/routes/export.sql (another agent is fixing export —
build compatibly with live-box export).
BUILD THE PDF LIFECYCLE MODULE — the "how a real app handles the PDFs":
- Three clear stages with a dedicated on-disk layout the module owns:
  data/source/ (immutable originals — ingest copies or references samples/),
  data/working/ (intermediate redacted working copies as review progresses),
  data/export/ (final exports). Migrate away from loose exports/ naming; keep
  compatibility (existing routes keep working).
- WORKING COPIES: a macro/route to materialize the CURRENT redaction state of a
  document as a working PDF (pdf_redact with the accepted boxes so far) —
  regenerate on demand, name-stamped (docN_workingK.pdf); the pdf extension's
  append-only incremental updates + pdf_revisions give each working copy a
  verifiable revision lineage. A registry table (CTAS/derived + the decisions
  pattern for mutations) tracks stage, path, fingerprint, created_ts, and the
  decision-batch it reflects.
- TEMP/MEMORY: document (in docs/pdf-lifecycle.md) + implement sensible temp
  handling: working copies are disposable/regenerable (a cleanup macro), source
  is never mutated, exports are only written by the export route.
- Route surface: GET /api/documents/:id/store (stages+fingerprints+revisions),
  POST /api/documents/:id/working (materialize working copy now).
VERIFY: boot; materialize a working copy after accepting boxes; pdf_revisions +
read_pdf prove the redaction state; cleanup works; existing routes unaffected.
Report evidence + the lifecycle doc.
