FIRST read the shared build contract in full: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md
Work in /Users/aloksubbarao/personal/closure.

YOU ARE THE DASHBOARD GROK (S1 in the design reference). You own ONLY:
server/templates/home.html, server/templates/case.html, static/dashboard.js.
Do NOT edit any .sql or other template.

Build the case dashboard to the reference's S1 quality:
- stat row (suggestions / pending / flagged / entities / signed-off), with the
  flagged stat styled as a warning.
- documents table: filename, found count, a confidence-breakdown mini bar
  (high/review/flagged proportions), status/progress; rows link to
  /documents/:id.
- an entities panel (decide once, propagates case-wide): each entity with
  instance count, docs, and pending/accepted pills; flagged-kind entities
  (CITATION/OFFICER/STREET NAME — the "NOT PII" kinds) get a "HUMAN REQUIRED"
  badge and are NOT bulk-acceptable.
- a coverage bar (accepted / pending / rejected proportions of the case).
- recent audit feed (GET /api/cases/:id/audit), newest first.
- an Export button that POSTs /api/cases/:id/export via static/dashboard.js; when
  the case still has pending flagged items, render the button DISABLED with the
  "Export blocked: N flagged items require individual judgment" banner + a "Jump
  to flagged" link (strings from design/copy.md). On success show the
  {exported, ...} result.
Read the CURRENT server/templates/case.html / home.html first for the tera
context vars already provided (case, stats, documents, entities, audit); keep
that contract, and state any new var you need at the end.

VERIFY: boot, open http://127.0.0.1:8117/ , confirm real numbers render, the docs
table links work, and the export button calls the route (show the JSON it got
back). Do not touch design/ or samples/.


VERIFY-PORT NOTE: other agents may hold :8117. Boot your OWN instance on port 8122: run  sed "s/8117/8122/" server/app.sql > /tmp/boot_dashboard.sql  then  /Users/aloksubbarao/personal/quackapi/build/release/duckdb -unsigned /tmp/dashboard.db -c ".read /tmp/boot_dashboard.sql" &  and curl/open http://127.0.0.1:8122 . Kill it when done.
