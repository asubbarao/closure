Work in /Users/aloksubbarao/personal/quackapi (the user's DuckDB HTTP-server extension).
BUG: src/quackapi_extension.cpp ApplyServeResourceGuards() hardcodes
  SET memory_limit TO '256MB'
on every quackapi_serve(). This silently caps the WHOLE database and caused OOM
500s in the app; a hardcoded cap that overrides the operator's own SET is wrong.

FIX: make it configurable and non-clobbering:
- Add a named parameter to quackapi_serve (e.g. memory_limit := '4GB') AND/OR an
  extension setting (SET quackapi_memory_limit='4GB') — follow the existing
  pattern used for cors_origins (named param wins, falls back to setting).
- Semantics: if the operator provided a value (param or setting), use it. If not,
  and the database already has a NON-default memory_limit set, DO NOT override
  it. Only apply the safe default (keep 256MB or pick something sane) when
  nothing was configured. Never silently clobber an explicit operator setting.
- Update the README's serve section to document it.
- Rebuild (make release) and run the extension test suite
  (./build/release/test/unittest "test/sql/*"); add a test for the new knob if
  the test format allows.
Report: the code change, test results, and the exact serve invocation the app
should use.
