THE SINGULAR REQUIREMENT of this product: a human must THOROUGHLY get through 800-3000 AI suggestions across 1000+ pages, fast. The funnel: ~3k suggestions -> high-confidence auto-pass + easy bulk groups clear ~2200 -> the human hand-reviews only the residual ~800, GROUPED (names, addresses, patterns) for batch judgment, while also catching false negatives. Everything must serve throughput + auditability.
FIRST read: /private/tmp/claude-501/-Users-aloksubbarao-personal/f15dcc22-ec83-4ec6-a926-096108b96135/scratchpad/contract.md and docs/detection-design.md and docs/ext-survey-detection.md
('ai' extension section). Work in /Users/aloksubbarao/personal/closure.
YOU OWN: spikes/llm-judge/** and docs/llm-judge.md ONLY (nice-to-have spike; do
not wire into server/).
SPIKE: LOCAL-LLM judges for flagged items. ollama is installed
(/usr/local/bin/ollama — check ollama list for available models; pull a small
one like llama3.2:3b or qwen2.5:3b if none). From DuckDB, call the local ollama
HTTP API (http_client extension http_post to localhost:11434) OR the 'ai'
community extension if it supports custom/base-url endpoints — pick whichever
works. For 5-10 real flagged suggestions from the app's data (boot or query
closure.db), send context + candidate to 2 different models/prompts as
independent judges: verdict redact/keep + one-line reason + score. Compare with
the deterministic judge ensemble's output.
docs/llm-judge.md: does it work, latency per item, quality vs the deterministic
judges, cost story (all-local), and the exact integration shape if we promote it
(a routes/judge_llm.sql sketch + when to run it — only on flagged residuals, async
queue via quackapi CREATE QUEUE?). Honest verdict: ship / demo-flag / skip.
