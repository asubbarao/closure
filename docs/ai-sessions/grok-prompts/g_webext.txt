Evaluate + spike where the DuckDB 'webbed' and 'crawler' community extensions add
GENUINE value to the redaction-review app at /Users/aloksubbarao/personal/closure.
Do NOT force-fit them. READ anything; WRITE only a new isolated spike dir
spikes/web-ingest/ (SQL + a short README) and docs/web-extensions-usage.md. Do
NOT edit server/*.sql, templates/, static/, or samples/ (other agents own those).

Extensions (study their real functions first: INSTALL webbed FROM community /
INSTALL crawler FROM community; webbed has html_to_json, xml_to_json,
html_extract_links, read_html; crawler crawls URLs). Confirm signed v1.5.x
osx_arm64 builds.

Two candidate fits — assess each honestly and, if real, build a SMALL working
spike proving it (standalone SQL in spikes/web-ingest/, not wired into the app):
1. MULTI-FORMAT DOCUMENTS: a redaction tool shouldn't be PDF-only. Use webbed to
   ingest an HTML and/or XML document — extract its text + (ideally) element
   offsets — into the SAME shape the app uses for PDFs (a words/tokens table with
   positions), so the same suggestion/redaction pipeline works on HTML/XML. Spike
   it on a sample HTML doc: show tokens extracted and that you could locate PII in
   them. Note how coordinates/positioning differ from PDF (char offsets vs boxes).
2. URL / SOURCE IMPORT: use crawler to fetch a document from a URL into the
   ingest pipeline (vs only local files). Spike a fetch + parse.

DELIVER docs/web-extensions-usage.md: for each fit — verdict (genuinely useful /
marginal / no), the mechanism, the spike result, and a recommendation on whether
to integrate it into the app (and roughly how) or leave it as a documented
capability. Be honest: if it's a stretch, say so. Report the spike outputs.
